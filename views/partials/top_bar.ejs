<div class="top-bar" data-top-bar>
    <%- include('../partials/icon_btn', { icon: 'chevron_left', variant: 'filled-tonal', dataAttr: 'data-back-btn' }) %>
    <%- include('../partials/icon_btn', { icon: 'chevron_right', variant: 'filled-tonal', dataAttr: 'data-forward-btn' }) %>
    <%- include('../partials/search_bar', { 
        display_name: locals.display_name,
        images: locals.images,
        query: locals.query
    }) %>
</div>
